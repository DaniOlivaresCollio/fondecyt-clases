# Descriptivos Clases - Chile

Tablas descriptivas para las variables de clases con Data Conclat Chile actualizada para el 08/10/2025.

## Preparación

```{r librerias, echo=FALSE, warning=FALSE, message=FALSE}
rm(list=ls())
options(scipen = 999)

# install.packages("pacman")
library(pacman)
p_load(
  # generacion docs 
  flextable,         # tablas personalizadas para word
  kableExtra,        # mejorar tablas
  knitr,             # generar reportes reproducibles
  officer,           # crear/modifcar documentos word o ppt
  tinytex,           # instalación mínima de LaTeX para generar PDF
  
  # importacion datos 
  haven,             # impotar/exportar datos spss, stata, sas con etiquetas
  readr,             # lectura rápida de csv
  readxl,            # leer excel 
  
  # manipulacion 
  forcats,           # manipulación de factores
  janitor,           # limpia etiquetas
  labelled,          # trabajar con variables etiquetadas
  purrr,             # programación funcional para listas y data frames
  sjlabelled,        # manejar etiquetas 
  stringr,           # manipular texto
  sjmisc,            # limpieza y descripción de datos
  
  # visualizacion
  ggplot2,           # visualización basado en gramática de gráficos
  ggrepel,           # etiquetas en gráficos sin superposición
  gridExtra,         # combinar gráficos en uno solo
  scales,            # controla cómo se ven los ejes en gráficos
  sjPlot,            # visualizar resultados estadísticos
  
  # analisis
  psych,             # análisis general, psicométrico
  survey,            # diseño survey encuestas complejas
  srvyr,             # diseño survey con tidy
  rlang,             # motor interno de tidyverse, para usar expresiones 
  tidyverse,
  statar             # conectar con el lenguaje stata

  # # conexion web
  # curl,              # conexiones http de bajo nivel: para solucionar el problema de la api del ine
  # httr               # cliente http para API
  )
```

### Cargar datos

```{r datos, message=FALSE, warning=FALSE}
data <- readRDS("output/data/datos-clases.rds")

# sjlabelled::get_label(data)
```

## Descriptivos

### Ingresos x Clase (09) sin ponderar

```{r}
prom_por_clase <- data %>%
  group_by(wrightclass1_reduced3) %>%
  summarise(
    mean_income = mean(income_rec, na.rm = TRUE),
    n           = n(),
    n_missing   = sum(is.na(income_rec)),
    .groups = "drop"
  )

prom_por_clase 
```

### Ingresos x Clase (10) sin ponderar

```{r}
prom_por_clase <- data %>%
  group_by(wrightclass2_reduced3) %>%
  summarise(
    mean_income = mean(income_rec, na.rm = TRUE),
    n           = n(),
    n_missing   = sum(is.na(income_rec)),
    .groups = "drop"
  )

prom_por_clase 
```

### Ingresos x Clase (09) ponderado

```{r}
data_svy <- as_survey_design(data, weights = rake_wb2)

prom_por_clase_w <- data_svy %>%
  group_by(wrightclass1_reduced3) %>%
  summarise(
    mean_income_w = survey_mean(income_rec, na.rm = TRUE, vartype = NULL),
    n_w           = survey_total(!is.na(income_rec), na.rm = TRUE, vartype = NULL),
    missing_w     = survey_total(is.na(income_rec), vartype = NULL),
    .groups = "drop"
  )

prom_por_clase_w
```

### Ingresos x Clase (10) ponderado

```{r}
data_svy <- as_survey_design(data, weights = rake_wb2)

prom_por_clase_w <- data_svy %>%
  group_by(wrightclass2_reduced3) %>%
  summarise(
    mean_income_w = survey_mean(income_rec, na.rm = TRUE, vartype = NULL),
    n_w           = survey_total(!is.na(income_rec), na.rm = TRUE, vartype = NULL),
    missing_w     = survey_total(is.na(income_rec), vartype = NULL),
    .groups = "drop"
  )

prom_por_clase_w
```

### Ingresos Employers (09) - Detalle

```{r}
data %>% 
  filter(as.numeric(wrightclass1_reduced3) == 1) %>% 
  select(id, wrightclass1_reduced3, income_rec) %>% 
  print(n = Inf)
```

### Ingresos Employers (09) - Frecuencia

```{r}
data %>% 
  filter(as.numeric(wrightclass1_reduced3) == 1) %>% 
  count(income_rec) %>% 
  mutate(pct = 100 * n / sum(n))
```

### Ingresos Capitalists (10) - Detalle

```{r}
data %>% 
  filter(as.numeric(wrightclass2_reduced3) == 1) %>% 
  select(id, wrightclass2_reduced3, income_rec) %>% 
  print(n = Inf)
```

### Ingresos Capitalists (10) - Frecuencia

```{r}
data %>% 
  filter(as.numeric(wrightclass2_reduced3) == 1) %>% 
  count(income_rec) %>% 
  mutate(pct = 100 * n / sum(n))
```

### Ingresos Small Employers (09) - Detalle

```{r}
data %>% 
  filter(as.numeric(wrightclass1_reduced3) == 2) %>% 
  select(id, wrightclass1_reduced3, income_rec) %>% 
  print(n = Inf)
```

### Ingresos Small Employers (09) - Frecuencia

```{r}
data %>% 
  filter(as.numeric(wrightclass1_reduced3) == 2) %>% 
  count(income_rec) %>% 
  mutate(pct = 100 * n / sum(n)) %>% 
  print(n = Inf)
```

### Ingresos Small Employers (10) - Detalle

```{r}
data %>% 
  filter(as.numeric(wrightclass2_reduced3) == 2) %>% 
  select(id, wrightclass2_reduced3, income_rec) %>% 
  print(n = Inf)
```

### Ingresos Small Employers (10) - Frecuencia

```{r}
data %>% 
  filter(as.numeric(wrightclass2_reduced3) == 2) %>% 
  count(income_rec) %>% 
  mutate(pct = 100 * n / sum(n)) %>% 
  print(n = Inf)
```

### a09 x Clase (10) sin ponderar 

```{r}
prom_por_clase <- data %>%
  group_by(wrightclass2_reduced3) %>%
  summarise(
    mean_partners = mean(a09, na.rm = TRUE),
    n             = n(),
    n_missing     = sum(is.na(a09)),
    .groups = "drop"
  )

prom_por_clase 
```

### a10 x Clase (09) sin ponderar 

```{r}
table(data$wrightclass1_reduced3, data$a10)
```

### a10 x Clase (10) sin ponderar 

```{r}
table(data$wrightclass2_reduced3, data$a10)
```


### a10 x Clase (11) sin ponderar 

```{r}
table(data$wrightclass1_reduced4, data$a10)
```
